{% extends 'base.html.twig' %}

{% block title %}Backoffice - Changer mot de passe{% endblock %}

{% block frontBack %}
    <a class="navbar-brand" href="{{ path('app_backoffice') }}">BACKOFFICE</a>
{% endblock %}

{% block body %}
    <h1>Changer le mot de passe</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-lg btn-block">{{ button_label|default('Changer') }}</button>
    {{ form_end(form) }}
{% endblock %}
